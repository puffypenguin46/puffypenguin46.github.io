// Project Title
// Your Name
// Date
//
// Extra for Experts:
// - describe what you did to take this project "above and beyond"


let x;
let y;
let sanya;

function preload() {
  sanya = createImg('sanya.png')
}

function preload() {
  up = createImg('up.png')
}

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
  sanya.position(900, 200)
  sanya.size(windowWidth / 4, windowHeight / 4)

  up.position(200, 100)
  up.size(windowWidth / 4, windowHeight / 4)
}





